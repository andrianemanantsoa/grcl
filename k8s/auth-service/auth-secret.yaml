apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secrets
  namespace: crowdauth
type: Opaque
stringData:
  # Replace this value before deployment (do not use placeholders in production).
  JWT_SECRET: "REPLACE_WITH_A_LONG_RANDOM_SECRET_VALUE"
