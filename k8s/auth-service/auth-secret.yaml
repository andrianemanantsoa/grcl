apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secrets
  namespace: crowdauth
type: Opaque
stringData:
  # For production, use `kubectl create secret ...` or Azure Key Vault + CSI driver instead of editing this file with real secrets.
  JWT_SECRET: "REPLACE_WITH_A_LONG_RANDOM_SECRET_VALUE"
